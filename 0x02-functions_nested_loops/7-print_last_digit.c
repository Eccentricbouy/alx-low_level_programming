#include "main.h"

/**
 * print_last_digit - check the code
 * @n: memeber
 * Return: Always 0.
 */
int print_last_digit(n)
  n = 50
{
int l;
l = n % 10;
if (i < 0)
{
_putchar(-l + 48);
return (-l);
}
else
{
_putchar(l + 48);
return (l);
}
}
