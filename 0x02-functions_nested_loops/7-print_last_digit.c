#include "main.h"
#include <stdio.h>

/**
 * print_last_digit - check the code
 * @n: memeber
 * Return: Always 0.
 */
int print_last_digit(n)
{
int l;
l = n % 10;
if (i < 0)
{
_putchar(-l + 48);
return (-l);
}
else
{
_putchar(l + 48);
return (l);
}
}
